import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'schengendayscalculator.micmax1000_afk.twa',
  appName: 'Schengen Days Calculator',
  webDir: 'dist'
};

export default config;
